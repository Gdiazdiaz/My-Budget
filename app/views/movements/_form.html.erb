<%= form_with(model: movement, url: {action: "create"}) do |form| %>
  <% if movement.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movement.errors.count, "error") %> prohibited this movement from being saved:</h2>

      <ul>
        <% movement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :name, placeholder: 'Name', class: 'input-tab' %>
  </div>

  <div>
    <%= form.number_field :amount, placeholder: 'Amount', class: 'input-tab' %>
  </div>

  <div>
    <%= form.collection_select :category_id, @groups, :id, :name, { prompt: "Category" }, class: 'select-tab' %>
  </div>

  <div class="actions btn-container">
    <div class='border'>
    <%= form.submit class: 'action-btn btn' %>
    </div>
  </div>
<% end %>
